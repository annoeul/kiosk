import { styled } from "styled-components"

export const LogoWrapper = styled.div`
  text-align: center;
`
export const Logo = styled.img`
  width: ${({ size }) => size}px;
  /* margin-top: 15%; */
  margin: 10% 0;
`
